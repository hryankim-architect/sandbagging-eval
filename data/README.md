# `data/`

`items.yaml` is the task set: verifiable, textbook-level questions each with a
single canonical `answer`. The eval measures whether ACCURACY drops as the eval
framing strengthens (sandbagging), not the facts. Adding an item is a reviewed,
PR-sized decision.
